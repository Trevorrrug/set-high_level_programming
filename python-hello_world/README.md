# Python Hello World

This project contains a simple Python script that prints "Best School".

## Files

- `0-run`: Runs the Python file stored in the `PYFILE` environment variable.
- `main.py`: Prints `Best School`.

## Usage

Set the Python filename:

```bash
export PYFILE=main.py